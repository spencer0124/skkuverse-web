/**
 * SDS Border Radius Tokens.
 *
 * Flutter source: lib/design/sds_radius.dart
 */
export const SdsRadius = {
  /** 4px — subtle rounding */
  xs: 4,
  /** 8px — buttons, inputs */
  sm: 8,
  /** 12px — card inner elements */
  md: 12,
  /** 14px — cards */
  lg: 14,
  /** 16px — bottom sheet top */
  xl: 16,
  /** 20px — filter chips, tab pills */
  full: 20,
} as const;
